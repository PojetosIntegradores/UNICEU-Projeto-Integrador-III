import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'UNICEU-Projeto-Integrador-III',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
